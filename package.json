{
  "name": "mcps-portal-api",
  "version": "1.0.0",
  "description": "Vercel serverless functions for MCPS Portal",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^17.4.1"
  }
}
