// FormBackend.gs

// CUSTOM_FORM_ID is kept solely for the one-time initPortalSchemaFromForm()
// migration function in PortalSchema.gs. Once migration is run you no longer
// need to touch the Google Form; everything goes through Portal_Schema sheet.
const CUSTOM_FORM_ID = "1DsNlu5Yc_PoAgy0ZYCtlTwNB3c4c5VURZPJ_X3MV3kQ";

// getFormMetadata()  → now in PortalSchema.gs
// submitCustomForm() → now in PortalSchema.gs